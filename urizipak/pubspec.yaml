name: urizipak
description: "우리지갑 — 함께 쓰고, 함께 모으고, 함께 키우는 커플·부부 공동 자산관리 플랫폼"
publish_to: 'none'

version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/characters/
    - assets/rooms/
    - assets/items/
